import React, { useState } from 'react'

const manageHints = activeClue => {

	// state
	const [currentHint, setCurrentHint] = useState(0);

	const 

	return { currentHint, setCurrentHint }
}

export default manageHints